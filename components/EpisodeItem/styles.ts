import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    borderRadius: 10,
    padding: 10,
    borderColor: '#ccc',
    borderWidth: 1,
    borderStyle: 'solid',
    marginBottom: 10,
  },
});

